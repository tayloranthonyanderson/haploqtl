# Dependencies for the diagnostic_variants.py script.
# genes_in_interval.py, gene_function.py, pubmed.py, and verify_report.py use only the
# Python standard library.
scikit-allel>=1.3.7
numpy>=1.24

# Optional: only for `verify_report.py --support-model` (the citation-support check in
# cite_support.py). The rest of the skill — including the verify gate — runs without it.
# anthropic>=0.40
